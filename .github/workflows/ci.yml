name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Run one line code
      run: echo azul Adel
    - name: Run two test
      run: |
        echo user ${{ secrets.VPS_USER }}
        